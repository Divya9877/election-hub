import { Voter, Booth, Officer, Assignment } from '@/types';

// Empty mock data arrays - data will be populated from MySQL via API
export const mockVoters: Voter[] = [];
export const mockBooths: Booth[] = [];
export const mockOfficers: Officer[] = [];
export const mockAssignments: Assignment[] = [];
